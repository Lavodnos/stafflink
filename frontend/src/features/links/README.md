# Links feature

Esta carpeta concentra la API, hooks y exports públicos del dominio de links.
- `api.ts`: tipos y funciones REST.
- `hooks.ts`: React Query para listar/crear/editar/estado.

Importa desde `@/features/links` para consumir en páginas o componentes.
